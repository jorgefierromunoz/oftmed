<%= form_for(enfermedadesoculare) do |f| %>
  <% if enfermedadesoculare.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(enfermedadesoculare.errors.count, "error") %> prohibited this enfermedadesoculare from being saved:</h2>

      <ul>
      <% enfermedadesoculare.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="col-md-6">
  <div class="form-group">
    <label for="nombre">Nombre</label>
        <%= f.text_field :nombre, class: 'form-control' %>
    
  </div>

  <div>
    <%= f.submit 'Guardar', class: 'btn btn-success' %>
  </div>
  <%= link_to 'Volver', enfermedadesoculares_path %>

  </div>
<% end %>
