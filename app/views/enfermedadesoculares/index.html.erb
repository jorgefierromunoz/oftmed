<% if notice != nil %>
<div class="alert alert-warning alert-dismissable">
  <button type="button" class="close" data-dismiss="alert">&times;</button>
  <p id="notice"><%= notice %></p>
</div>
<% end %>

<h3>Enfermedades oculares</h3>

<%= link_to '+ Agregar Enfermedad ocular', new_enfermedadesoculare_path %>
<table class="table table-hover">
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Editar</th>
      <th>Eliminar</th>
    </tr>
  </thead>

  <tbody>
    <% @enfermedadesoculares.each do |enfermedadesoculare| %>
      <tr>
        <td><%= enfermedadesoculare.nombre %></td>
        <td><%= link_to 'Editar', edit_enfermedadesoculare_path(enfermedadesoculare) %></td>
        <td><%= link_to 'Eliminar', enfermedadesoculare, method: :delete, data: { confirm: '¿Estás seguro que desea eliminar este registro?', id: enfermedadesoculare.id, controlador: 'enfermedadesoculares' }, class: 'eliminarbtn' %>
          
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

